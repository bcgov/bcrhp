name: Release package to GitHub Packages

on:
  pull_request:
    types: [closed]
    branches:
      - 'main'
      - 'dev/*'
      - 'release/*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    if: >
      github.event.pull_request.merged == true &&
      startsWith(github.event.pull_request.title, 'on merge: github release of version')
    uses: bcgov/bcrhp/.github/workflows/release_deploy_common.yml@release/1.4.0_auto_deploy
