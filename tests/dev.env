#arches
ARCHES_PROJECT=bcrhp
ARCHES_CORE_VERSION=7-6-4
APP_ENV=dev
APP_SUFFIX=_${APP_ENV}

PROJECT_GROUP_NAME=bcgov
INSTALL_DEFAULT_GRAPHS=False
INSTALL_DEFAULT_CONCEPTS=False

# Postgres Config
PGUSERNAME=postgres
PGPASSWORD=postgis
#PGDBNAME=$ARCHES_PROJECT
PGDBNAME=bcrhp
PGHOST=postgres
PGPORT=5432

CACHE_BACKEND=django.core.cache.backends.dummy.DummyCache
CACHE_BACKEND_LOCATION=

# Elasticsearch config
ES_HOST=localhost
ES_SCHEME=http
ES_PORT=9200
ES_API_KEY=
ES_CERT_FILE=


DJANGO_MODE=DEV
DJANGO_DEBUG=True
DOMAIN_NAMES=*
DJANGO_PORT=80
#DJANGO_REMOTE_DEBUG=False
PYTHONUNBUFFERED=0
TZ=PST
ELASTICSEARCH_PREFIX=$ARCHES_PROJECT

ALLOWED_HOSTS=*
AUTH_BYPASS_HOSTS=localhost
# Used by webpack for data export
PUBLIC_SERVER_ADDRESS=http://localhost:8000/

#rabbitmq
RABBITMQ_USER=guest
RABBITMQ_PASS=guest

# Celery
CELERY_WORKER_NAME=bcrhp_dev
CELERY_BROKER_URL=redis://redis_arches7-5-2:6379/0

#cantaloupe
CANTALOUPE_ENDPOINT_ADMIN_ENABLED=true
CANTALOUPE_ENDPOINT_ADMIN_USERNAME=admin
CANTALOUPE_ENDPOINT_ADMIN_SECRET=admin
CANTALOUPE_HOST=cantaloupe_${ARCHES_PROJECT}
CANTALOUPE_PORT=8182
CANTALOUPE_FILESYSTEMSOURCE_BASICLOOKUPSTRATEGY_PATH_PREFIX=/imageroot/

## BCGov specific values
# Prefix when used behind a reverse proxy
# Must end with a / but have no leading /
BCGOV_PROXY_PREFIX=bcrhp/

#DJANGO KEY
#DJANGO_SECRET_KEY=
FORCE_SCRIPT_NAME=None # None for dev, /prefix/ when behind Apache

#HRIA connection
HRIADB_USER=
HRIADB_PASSWORD=
HRIADB_HOST=
HRIADB_PORT=
HRIADB_SERVICE_NAME=
HRIADB_APPLICATION_USER=

#S3 info
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_PROXIES=

# OAUTH/OIDC configuration
OAUTH_AUTH_ENDPOINT=
OAUTH_TOKEN_ENDPOINT=
OAUTH_JWKS_URI=
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_DOMAINS=

#Only for dev!!!!
OAUTHLIB_INSECURE_TRANSPORT=

#Local Tileserver config
TILESERVER_OUTBOUND_PROXY=
